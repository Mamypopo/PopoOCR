# PopoOCR - ภาพเป็นข้อความ (OCR)

เว็บแอปพลิเคชัน OCR (Optical Character Recognition) ที่ทันสมัย แปลงภาพเป็นข้อความด้วย AI รองรับภาษาไทยและอังกฤษ

## ✨ ฟีเจอร์

- 🎨 **UI/UX ทันสมัย** - ดีไซน์แบบ SaaS 2024 พร้อม Glass Effect
- 🌓 **Dark Mode** - รองรับทั้ง Auto-detect และ Manual Toggle
- 📱 **Responsive** - ใช้งานได้ทั้งมือถือและ Desktop
- 🖼️ **Drag & Drop** - ลากวางไฟล์ได้ง่าย
- 📄 **รองรับ PDF** - แปลง PDF เป็นภาพแล้วทำ OCR อัตโนมัติ (ปรับปรุงความแม่นยำด้วย Image Preprocessing)
- 📊 **Progress Tracking** - แสดงความคืบหน้าการประมวลผลแบบ Real-time
- 📋 **Copy to Clipboard** - คัดลอกข้อความได้ทันที
- 🇹🇭 **รองรับภาษาไทย** - ใช้ Tesseract.js รองรับภาษาไทยและอังกฤษ
- 📝 **อ่านเฉพาะข้อความ** - อ่านเฉพาะข้อความเท่านั้น ไม่ตรวจจับรูปภาพ QR Code หรือบาร์โค้ด

## 🚀 เทคโนโลยีที่ใช้

- **React 18** - UI Framework
- **Vite** - Build Tool
- **Tailwind CSS** - Styling
- **Tesseract.js** - OCR Engine
- **pdfjs-dist** - PDF to Image Converter
- **Lucide Icons** - Icon Library
- **Headless UI** - UI Components

## 📦 การติดตั้ง

1. ติดตั้ง dependencies:

```bash
npm install
```

2. รันโปรเจคในโหมด Development:

```bash
npm run dev
```

3. Build สำหรับ Production:

```bash
npm run build
```

4. Preview Production Build:

```bash
npm run preview
```

## 🌐 การ Deploy บน Vercel

1. Push โค้ดไปยัง GitHub Repository

2. ไปที่ [Vercel](https://vercel.com) และ Sign in

3. คลิก "New Project" และเลือก Repository ของคุณ

4. Vercel จะตรวจจับ Vite อัตโนมัติ - คลิก "Deploy"

5. รอให้ Deploy เสร็จ (ประมาณ 1-2 นาที)

6. ได้ URL สำหรับเว็บไซต์ของคุณแล้ว! 🎉

### หรือใช้ Vercel CLI:

```bash
npm i -g vercel
vercel
```

## 📁 โครงสร้างโปรเจค

```
PopoOCR/
├── src/
│   ├── components/
│   │   ├── Navbar.jsx          # Navigation Bar + Dark Mode Toggle
│   │   ├── Hero.jsx            # Hero Section
│   │   ├── ImageUpload.jsx     # Drag & Drop Upload Component
│   │   ├── OCRProcessor.jsx    # OCR Processing with Progress
│   │   └── OCRResult.jsx       # Result Display + Copy/Reset
│   ├── contexts/
│   │   └── ThemeContext.jsx    # Dark Mode Context
│   ├── utils/
│   │   ├── pdfToImage.js       # PDF to Image Converter
│   │   ├── imagePreprocessing.js # Image Enhancement สำหรับ OCR
│   │   └── textPostProcessing.js # Text Post-Processing และ Quality Scoring
│   ├── App.jsx                 # Main App Component
│   ├── main.jsx                # Entry Point
│   └── index.css               # Global Styles
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
└── postcss.config.js
```

## 🎨 การใช้งาน

1. **อัปโหลดไฟล์**: ลากวางไฟล์หรือคลิกเพื่อเลือกภาพ/PDF
2. **แปลง PDF** (ถ้าเป็น PDF): ระบบจะแปลงหน้าแรกของ PDF เป็นภาพอัตโนมัติ
3. **รอประมวลผล OCR**: ระบบจะแสดง Progress Bar และเปอร์เซ็นต์ความคืบหน้า
4. **ดูผลลัพธ์**: ข้อความที่แปลงได้จะแสดงในกล่องผลลัพธ์
5. **คัดลอก**: คลิกปุ่ม "คัดลอก" เพื่อคัดลอกข้อความ
6. **เริ่มใหม่**: คลิกปุ่ม "เริ่มใหม่" เพื่อรีเซ็ตและอัปโหลดไฟล์ใหม่

## 🔧 การปรับแต่ง

### เปลี่ยนฟอนต์

แก้ไขใน `tailwind.config.js`:

```js
fontFamily: {
  sans: ['Inter', 'Plus Jakarta Sans', 'Prompt', 'system-ui', 'sans-serif'],
}
```

### เปลี่ยนสีธีม

แก้ไขใน `tailwind.config.js`:

```js
colors: {
  primary: {
    // เปลี่ยนสีตามต้องการ
  }
}
```

## 📝 หมายเหตุ

- Tesseract.js จะดาวน์โหลด Worker Files ครั้งแรกที่ใช้งาน (ประมาณ 2-3 MB)
- pdfjs-dist จะใช้ CDN Worker สำหรับแปลง PDF เป็นภาพ
- การประมวลผล OCR ใช้เวลาขึ้นอยู่กับขนาดและความซับซ้อนของภาพ/PDF
- รองรับไฟล์ภาพ: PNG, JPG, JPEG, WEBP
- รองรับไฟล์ PDF: จะแปลงหน้าแรกเป็นภาพแล้วทำ OCR (สามารถขยายเป็นหลายหน้าได้ในอนาคต)
- **อ่านเฉพาะข้อความ**: ระบบจะอ่านเฉพาะข้อความเท่านั้น ไม่ตรวจจับหรืออ่านรูปภาพ QR Code หรือบาร์โค้ด

## 🎯 การปรับปรุงความแม่นยำ OCR สำหรับ PDF (Enhanced Mode)

ระบบมีการปรับปรุงความแม่นยำสำหรับ PDF แบบเข้มข้น (Enhanced Mode) โดย:

### 1. **เพิ่มความละเอียดสูงสุด**
- ใช้ scale **4.0** (เพิ่มจาก 3.0) เมื่อแปลง PDF เป็นภาพ
- ภาพมีความละเอียดสูงมาก ช่วยให้ OCR ทำงานได้แม่นยำขึ้น

### 2. **Image Preprocessing แบบเข้มข้น**
- ✅ **Contrast Enhancement**: ปรับ contrast สูงขึ้น (1.6)
- ✅ **Brightness Adjustment**: เพิ่ม brightness (30)
- ✅ **Grayscale Conversion**: แปลงเป็น grayscale
- ✅ **Noise Reduction**: ลดสัญญาณรบกวนด้วย Median Filter
- ✅ **Sharpen (Enhanced)**: เพิ่มความคมชัดแบบเข้มข้น
- ✅ **Binarization (Otsu)**: แปลงเป็นขาวดำด้วย Otsu's Method สำหรับความคมชัดสูงสุด

### 3. **Multi-PSM Mode Testing (ใหม่!)**
- **ทดสอบหลาย PSM Modes**: ระบบจะลอง PSM modes หลายแบบ (3, 6, 11, 4)
- **เลือกผลที่ดีที่สุด**: ใช้ Confidence Score + Text Quality Score เพื่อเลือกผลลัพธ์ที่ดีที่สุด
- **อัตโนมัติ**: ไม่ต้องตั้งค่าเอง ระบบจะเลือกให้อัตโนมัติ
- **เหมาะกับทุกประเภท**: ทำงานได้ดีทั้ง PDF และภาพปกติ

### 4. **OCR Settings ที่ปรับแต่งแล้ว**
- **LSTM OCR Engine**: ใช้ Neural Network-based OCR
- **Preserve Interword Spaces**: รักษาช่องว่างระหว่างคำ
- **Optimized Parameters**: ปรับแต่ง parameters สำหรับผลลัพธ์ที่ดีที่สุด

### 5. **Text Post-Processing (ใหม่!)**
- **Text Cleaning**: ทำความสะอาดข้อความ (ลบอักขระควบคุม, แก้ไขช่องว่าง)
- **Error Correction**: แก้ไขข้อผิดพลาดทั่วไปที่เกิดจาก OCR
- **Quality Scoring**: คำนวณคะแนนคุณภาพของข้อความเพื่อเลือกผลที่ดีที่สุด

### 6. **Preprocessing สำหรับภาพปกติ**
- ใช้ preprocessing แบบเบา (Standard Mode)
- ปรับ contrast/brightness แบบพอดี
- ใช้ sharpen แบบปกติ
- Multi-PSM testing เหมือนกัน แต่ใช้ PSM modes ที่เหมาะกับภาพ

### 💡 เคล็ดลับเพิ่มความแม่นยำ

หากผลลัพธ์ยังไม่แม่นยำพอ:
- ✅ ตรวจสอบว่า PDF มีความชัดเจนเพียงพอ
- ✅ PDF ที่สแกนจากเอกสารจริง: ระบบจะใช้ Enhanced Mode อัตโนมัติ
- ✅ PDF ที่เป็น text-based (ไม่ใช่ภาพ): อาจไม่ต้องใช้ OCR
- ✅ PDF ที่มีคุณภาพต่ำ: ระบบจะปรับปรุงภาพอัตโนมัติ
- ✅ PDF ที่มี layout ซับซ้อน: ใช้ PSM 3 (Auto detect) เพื่อความแม่นยำสูงสุด

### ⚡ Performance Note
- Enhanced Mode + Multi-PSM Testing ใช้เวลาประมวลผลนานขึ้น (ประมาณ 2-3 เท่า)
- แต่ให้ผลลัพธ์ที่แม่นยำกว่ามากมาก
- เหมาะสำหรับ PDF ที่ต้องการความแม่นยำสูงสุด
- ระบบจะทดสอบ 4 PSM modes แล้วเลือกผลที่ดีที่สุดอัตโนมัติ

### 🚀 เทคนิคขั้นสูงที่ใช้

1. **Multi-PSM Testing**: ลองหลาย PSM modes (3, 6, 11, 4) แล้วเลือกผลที่ดีที่สุด
2. **Confidence-Based Selection**: ใช้ Confidence Score จาก Tesseract
3. **Text Quality Scoring**: คำนวณคะแนนคุณภาพของข้อความ (ความยาว, ความหลากหลาย, อัตราส่วน)
4. **Smart Result Selection**: เลือกผลลัพธ์ที่ดีที่สุดโดยพิจารณาทั้ง Confidence และ Quality
5. **Text Post-Processing**: ทำความสะอาดและแก้ไขข้อผิดพลาดทั่วไป

## 📄 License

MIT

---

สร้างด้วย ❤️ โดยใช้ React + Vite + Tesseract.js

